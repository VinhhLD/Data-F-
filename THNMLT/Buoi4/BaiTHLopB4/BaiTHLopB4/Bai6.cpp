#define _CRT_NONSTDC_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*#include <conio.h>
#include <math.h>
int snt (int n)
{
    if(n < 2) 
		return 0;
    for(int i = 2; i <= sqrt((float)n); i++)
        if(n % i == 0)
        return 0;
    return 1;
} 
int main ()
{
  int n;
  int tong = 0;
  printf("Nhap vao n: ");
  scanf("%d", &n);
  for(int i = 1; i < n; i++)
    if(snt(i))
        tong = tong + i;
    printf("Tong cac so nguyen to < %d = %d\n", n, tong);
	return 0;
	getch ();
}*/