/*#include <stdio.h>
#include <conio.h>
#include <math.h>
int main ()
{
	int x;
	float F;
	printf("Nhap so nguyen x: "); scanf_s("%d", &x);
	F = (3 * pow(x, 5.0) + 2 * x + sqrt(x + 1.0)) / (5 * pow(x, 2.0) - 3);
	printf("F(x) = %.3f", F);
	return 0;
	getch ();
}*/
